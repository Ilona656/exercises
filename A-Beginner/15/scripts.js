// 15. Skriv en funktion som multiplicerar två tal och returnerar resultatet
function multiply(x, y) {
    return x * y
}

const a = 5
const b = 7

const produkt = multiply(a, b)

console.log(a + '*' + b + '=' + produkt)